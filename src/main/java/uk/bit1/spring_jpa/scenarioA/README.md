# Variant 1 — FK in Customer table (Customer owns FK) — bidirectional

# @OneToOne mapping style 1: Foreign Key column with Unique constraint

Customer is Parent, Profile is Child
Customer is Owner, Profile is Inverse
Bidirectional

