# Variant 2 — FK in Profile table (Profile owns FK) — bidirectional
Variant 2 — FK in Profile table (Profile owns FK) — bidirectional

Customer is Parent, Profile is Child
Profile is Owner, Customer is Inverse
Bidirectional

